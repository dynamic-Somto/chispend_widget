name: chispend_widget
description: A new Flutter project.
version: 0.0.1
homepage: https://github.com/dynamic-Somto/chispend_widget

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
